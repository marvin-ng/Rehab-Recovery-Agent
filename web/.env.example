# Function URLs for the deployed AWS backend. These are PUBLIC endpoints gated
# by the x-api-key header — not secrets. The API key itself is NEVER stored
# here; it is entered by the user at runtime and kept only in localStorage.
VITE_LOG_SESSION_URL=https://6owlutqcgqvjnvbaenmywy2dgi0qvbjr.lambda-url.us-east-1.on.aws/
VITE_DASHBOARD_URL=https://bsve7qr3msascmtlcmfng5rbsy0uoudi.lambda-url.us-east-1.on.aws/
